on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run_Xcodebuild:
    name: build and test
    runs-on: macos-latest
    steps: 
    - name: mxcl/Xcodebuild
      uses: mxcl/xcodebuild@v2.0.0
      with:
        working-directory: /Users/runner/work/BoldlyGiffing
