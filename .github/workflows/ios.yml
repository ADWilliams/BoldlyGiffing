on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run_Xcodebuild:
    name: build and test
    runs-on: macos-latest
    steps: 
    - name: Xcodebuild Action
      uses: sersoft-gmbh/xcodebuild-action@v2.0.1
      with: 
        project: BoldlyGiffing.xcodeproj
